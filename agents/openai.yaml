interface:
  display_name: "OTL"
  short_description: "Design judgment, interaction craft, and quality gates"
  brand_color: "#1A1A1A"
  default_prompt: "Use $otl to shape and implement this interface from product context, then run the visual evidence and implementation quality gates."

policy:
  allow_implicit_invocation: true
