interface:
  display_name: "OTL"
  short_description: "Design judgment and rendered visual QA"
  brand_color: "#1A1A1A"
  default_prompt: "Use $otl to shape and implement this interface from product context, then review the rendered result visually."

policy:
  allow_implicit_invocation: true
